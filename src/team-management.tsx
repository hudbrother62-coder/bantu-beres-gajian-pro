import { useEffect, useState } from 'react'
import type { FormEvent } from 'react'
import { Plus } from 'lucide-react'
import { supabase } from './lib/supabase'

type Profile={id:string;company_id:string;full_name:string;username:string;role:string;is_active?:boolean;job_title?:string|null;employees?:Employee[]}
type Employee={id:string;employee_code:string;full_name:string;department?:string|null;position?:string|null;is_active:boolean}
const roles:Record<string,string>={owner:'Owner',hr_admin:'HR / Admin',finance:'Keuangan',supervisor:'Atasan',employee:'Karyawan'}
function Field({label,children}:{label:string;children:React.ReactNode}){return <label className="field"><span>{label}</span>{children}</label>}
function Modal({title,close,children}:{title:string;close:()=>void;children:React.ReactNode}){return <div className="modal-layer"><button className="backdrop" onClick={close}/><section className="modal"><header><h3>{title}</h3><button className="button secondary" onClick={close}>Tutup</button></header>{children}</section></div>}
export function TeamManagement({profile}:{profile:Profile}){const canManage=['owner','hr_admin'].includes(profile.role),isOwner=profile.role==='owner';const [rows,setRows]=useState<Profile[]>([]),[employees,setEmployees]=useState<Employee[]>([]),[open,setOpen]=useState(false),[editing,setEditing]=useState<Profile|null>(null),[message,setMessage]=useState(''),[busy,setBusy]=useState(false),[selectedEmployee,setSelectedEmployee]=useState('');const load=async()=>{const [profiles,employeeRows]=await Promise.all([supabase.from('profiles').select('*, employees(id,employee_code,full_name,department,position,is_active)').order('created_at'),supabase.from('employees').select('id,employee_code,full_name,department,position,is_active').eq('is_active',true).is('profile_id',null).order('full_name')]);setRows((profiles.data||[])as Profile[]);setEmployees((employeeRows.data||[])as Employee[])};useEffect(()=>{void load()},[]);const invoke=async(body:any,success:string)=>{setBusy(true);const {data,error}=await supabase.functions.invoke('create-team-member',{body});setBusy(false);if(error||data?.error){setMessage(data?.error||'Perubahan akun tim belum dapat diproses.');return false}setMessage(success);await load();return true};const add=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const body:any=Object.fromEntries(new FormData(e.currentTarget).entries());if(!isOwner)body.role='employee';if(await invoke(body,selectedEmployee?'Akun Karyawan dibuat dan dihubungkan ke data karyawan.':'Akun tim berhasil dibuat.')){setOpen(false);setSelectedEmployee('')}};const saveEdit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();if(!editing)return;const body:any={action:'update-team',profileId:editing.id,...Object.fromEntries(new FormData(e.currentTarget).entries())};if(!isOwner)body.role='employee';if(await invoke(body,'Data akun tim berhasil diperbarui.'))setEditing(null)};const setActive=async(row:Profile,active:boolean)=>{if(await invoke({action:'set-active',profileId:row.id,active},active?'Akun tim diaktifkan kembali.':'Akun tim dinonaktifkan.'))setEditing(null)};const remove=async(row:Profile)=>{if(!confirm(`Hapus akun tim @${row.username}? Data karyawan dan histori tidak ikut dihapus.`))return;if(await invoke({action:'delete-team',profileId:row.id},'Akun tim berhasil dihapus.'))setEditing(null)};const picked=employees.find(row=>row.id===selectedEmployee);const manageable=(row:Profile)=>row.role!=='owner'&&(isOwner||row.role==='employee');return <section className="page"><div className="intro"><div><p className="eyebrow">Akses perusahaan</p><h1>Tim</h1><p>Owner dapat mengelola semua role. HR/Admin hanya dapat membuat dan mengelola akun role Karyawan.</p></div>{canManage&&<button className="button primary" onClick={()=>setOpen(true)}><Plus/>{isOwner?'Tambah tim':'Tambah akun Karyawan'}</button>}</div>{message&&<p className="note" role="status">{message}</p>}<div className="team">{rows.map(r=>{const linked=Array.isArray(r.employees)?r.employees[0]:null;return <article className="card member" key={r.id}><div className="avatar">{(linked?.full_name||r.full_name)[0]}</div><div><h3>{linked?.full_name||r.full_name}</h3><p>@{r.username}</p><div className="inline-actions"><span className="badge">{roles[r.role]}</span><span className={r.is_active===false?'badge rejected':'badge active'}>{r.is_active===false?'Akun nonaktif':'Akun aktif'}</span></div><small>{linked?`${linked.employee_code} · ${linked.position||linked.department||'Data jabatan belum lengkap'}`:r.role==='employee'?'Belum terhubung ke data karyawan':'Akun manajemen'}</small>{manageable(r)&&<div className="workflow-actions"><button className="button secondary" onClick={()=>setEditing(r)}>Edit</button><button className="button secondary" disabled={busy} onClick={()=>void setActive(r,r.is_active===false)}>{r.is_active===false?'Aktifkan':'Nonaktifkan'}</button><button className="button secondary" disabled={busy} onClick={()=>void remove(r)}>Hapus akun</button></div>}</div></article>})}</div>{open&&<Modal title={isOwner?'Tambah anggota tim':'Tambah akun Karyawan'} close={()=>setOpen(false)}><form onSubmit={add} className="form"><Field label="Hubungkan ke karyawan aktif"><select name="employeeId" value={selectedEmployee} onChange={e=>setSelectedEmployee(e.target.value)}><option value="">Buat data karyawan draft baru</option>{employees.map(row=><option key={row.id} value={row.id}>{row.full_name} · {row.employee_code}</option>)}</select></Field>{picked&&<p className="note">Akun akan memakai data {picked.full_name}.</p>}<div className="two"><Field label="Nama lengkap"><input name="fullName" required={!selectedEmployee} defaultValue={picked?.full_name||''}/></Field><Field label="Jabatan"><input name="jobTitle" defaultValue={picked?.position||''}/></Field></div><div className="two"><Field label="Username"><input name="username" minLength={4} required/></Field><Field label="Password awal"><input name="password" type="password" minLength={8} required/></Field></div>{isOwner?<Field label="Role"><select name="role" defaultValue="employee"><option value="employee">Karyawan</option><option value="hr_admin">HR / Admin</option><option value="finance">Keuangan</option><option value="supervisor">Atasan</option></select></Field>:<p className="note">Role dikunci sebagai Karyawan untuk akun yang dibuat HR/Admin.</p>}<div className="two"><Field label="Kode karyawan baru"><input name="employeeCode" placeholder="EMP-002"/></Field><Field label="Divisi"><input name="department" placeholder="Operasional"/></Field></div><Field label="Jabatan karyawan"><input name="position" placeholder="Staff"/></Field><button className="button primary" disabled={busy}>{busy?'Menyimpan…':'Buat akun'}</button></form></Modal>}{editing&&<Modal title={`Edit @${editing.username}`} close={()=>setEditing(null)}><form className="form" onSubmit={saveEdit}><Field label="Nama lengkap"><input name="fullName" defaultValue={editing.full_name} required/></Field><Field label="Username"><input name="username" defaultValue={editing.username} minLength={4} required/></Field><Field label="Jabatan"><input name="jobTitle" defaultValue={editing.job_title||''}/></Field>{isOwner?<Field label="Role"><select name="role" defaultValue={editing.role}><option value="employee">Karyawan</option><option value="hr_admin">HR / Admin</option><option value="finance">Keuangan</option><option value="supervisor">Atasan</option></select></Field>:<p className="note">HR/Admin hanya dapat mempertahankan role Karyawan.</p>}<Field label="Password baru (opsional)"><input name="password" type="password" minLength={8}/></Field><button className="button primary" disabled={busy}>Simpan perubahan</button></form></Modal>}</section>}
